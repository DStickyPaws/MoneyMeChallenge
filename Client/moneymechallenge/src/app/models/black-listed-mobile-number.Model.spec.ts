import { BlackListedMobileNumber } from './black-listed-mobile-number.Model';

describe('BlackListedMobileNumber', () => {
  it('should create an instance', () => {
    expect(new BlackListedMobileNumber()).toBeTruthy();
  });
});
